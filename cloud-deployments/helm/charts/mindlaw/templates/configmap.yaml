apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "mindlaw.labels" . | nindent 4 }}
  name: {{ include "mindlaw.fullname" . }}-config
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
{{- end }}